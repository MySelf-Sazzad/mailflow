import { MarketingPage } from "@/components/marketing/MarketingPage";
const items=[["Private bulk sending","Every recipient receives an individual message. Addresses are never exposed to other recipients."],["Contacts and lists","Import CSV or XLSX files, remove duplicates, validate addresses, and organise subscribers."],["Personalisation","Merge names, companies, emails, and phone details into subjects and content."],["Scheduling and queues","Schedule delivery while durable jobs process separately with retries and failure records."],["Responsible analytics","Monitor delivery, opens, clicks, bounces, failures, complaints, and unsubscribes."],["Safety controls","Verified accounts, plan limits, suppression lists, signed unsubscribe links, and admin oversight."]];
export default function Features(){return <MarketingPage eyebrow="Platform features" title="Everything needed for responsible email campaigns" intro="Build, send, and understand campaigns from one secure workspace."><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{items.map(([t,d])=><article className="card p-6" key={t}><h2 className="section-title">{t}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{d}</p></article>)}</div></MarketingPage>}
