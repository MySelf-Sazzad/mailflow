import { prisma } from "@/lib/prisma";
export default async function QueuePage(){const jobs=await prisma.queueJob.findMany({orderBy:{createdAt:"desc"},take:100});return <div className="space-y-6"><h1 className="page-title">Email queue</h1><div className="card overflow-x-auto"><table className="data-table"><thead><tr><th>Job</th><th>Status</th><th>Attempts</th><th>Error</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td>{j.id}</td><td>{j.status}</td><td>{j.attempts}/{j.maxAttempts}</td><td className="max-w-sm truncate">{j.lastError??"—"}</td></tr>)}</tbody></table></div></div>}
