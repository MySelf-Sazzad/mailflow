"use client";
import { useActionState } from "react";import { updateAdminCredentials,type AdminCredentialState } from "@/app/actions";
const initial:AdminCredentialState={};
export function AdminCredentialForm({email}:{email:string}){const [state,action,pending]=useActionState(updateAdminCredentials,initial);return <form action={action} autoComplete="off" className="card max-w-2xl space-y-5 p-6"><div><h2 className="section-title">Administrator login</h2><p className="page-subtitle">Email and password changes require the current password. Passwords are stored only as bcrypt hashes.</p></div><label className="label">Current admin email<input className="input mt-1 bg-slate-50" value={email} readOnly tabIndex={-1}/></label><label className="label">New admin email<input className="input mt-1" name="newEmail" type="email" defaultValue={email} autoComplete="off" required/></label><label className="label">Current password<input className="input mt-1" name="currentPassword" type="password" autoComplete="current-password" required/></label><div className="grid gap-4 md:grid-cols-2"><label className="label">New password (optional)<input className="input mt-1" name="newPassword" type="password" autoComplete="new-password"/></label><label className="label">Confirm new password<input className="input mt-1" name="confirmPassword" type="password" autoComplete="new-password"/></label></div>{state.error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}{state.success&&<p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{state.success}</p>}<button disabled={pending} className="btn-primary">{pending?"Updating...":"Save admin login"}</button></form>}
