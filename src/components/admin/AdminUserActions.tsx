"use client";
import { adminUpdateUser } from "@/app/actions";
export function AdminUserActions({userId,status,plans}:{userId:string;status:string;plans:{slug:string;name:string}[]}){return <form action={adminUpdateUser} onSubmit={e=>{const action=new FormData(e.currentTarget).get("action");if(action==="delete"&&!confirm("Permanently delete this user and all of their data?"))e.preventDefault()}} className="flex min-w-72 gap-2"><input type="hidden" name="userId" value={userId}/><select name="action" className="input"><option value="activate">Activate user</option><option value="suspend">Suspend user</option><option value={status==="BLOCKED"?"activate":"block"}>{status==="BLOCKED"?"Unban user":"Ban user"}</option>{plans.map(p=><option key={p.slug} value={`plan:${p.slug}`}>Set {p.name} plan</option>)}<option value="delete">Delete permanently</option></select><button className="btn-primary">Apply</button></form>}
